{
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "ngrok": "ngrok start --all --config ngrok.yml",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\" \"npm run ngrok\""
  }
}